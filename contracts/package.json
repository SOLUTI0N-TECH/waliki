{
  "name": "@waliki/contracts",
  "private": true,
  "version": "0.1.0",
  "description": "Contratos de Waliki: TestUSDT + WalikiRouter (Base Sepolia)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:baseSepolia": "hardhat run scripts/deploy.ts --network baseSepolia && npm run export-abi",
    "export-abi": "ts-node scripts/export-abi.ts",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "@types/chai": "^4.3.20",
    "@types/chai-as-promised": "^7.1.8",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.10.0",
    "chai-as-promised": "^7.1.2",
    "dotenv": "^16.4.7",
    "hardhat": "^2.24.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
